import { useEffect } from "react";

const AppMetadata = () => {
  useEffect(() => {
    // No operations to perform in useEffect after removing meta viewport and style elements
  }, []);

  return null;
};

export default AppMetadata;